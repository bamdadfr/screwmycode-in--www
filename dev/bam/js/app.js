
function update () {
  // scene.children[2].position.x += 0.01
  // console.log(scene.children[2].position.x)

  // scene.children[2].children[1].rotation.x += 0.01
  // scene.children[2].children[1].rotation.y += 0.01

  // scene.children[2].children[1].rotation.y -= 0.01
  // scene.children[2].children[1].rotation.z -= 0.01

  // console.log(scene.children[2].position.x)

}

window.addEventListener('resize', onWindowResize)

init()

// console.log(scene.children[2])

